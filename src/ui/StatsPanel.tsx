// Player stats placeholder. Phase 3.2 will fill in HP/mana/endurance/exp
// bars and the STR/STA/AGI/DEX/WIS/INT/CHA grid once the daemon emits
// PlayerStats events.
export function StatsPanel() {
  return (
    <div className="px-2 py-2 text-xs text-neutral-500">
      Stats panel — coming next.
    </div>
  );
}
