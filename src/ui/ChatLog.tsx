// Chat log placeholder. Phase 3.3 will subscribe to ChatMessage events
// and render a scrolling pane with per-channel coloring.
export function ChatLog() {
  return (
    <div className="px-2 py-2 text-xs text-neutral-500">
      Chat log — coming after stats.
    </div>
  );
}
