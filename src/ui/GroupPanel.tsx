// Group panel placeholder. Phase 3.4 will fill in 6 group slots with
// HP%/distance once the daemon emits GroupUpdate events.
export function GroupPanel() {
  return (
    <div className="px-2 py-2 text-xs text-neutral-500">
      Group panel — coming after chat.
    </div>
  );
}
